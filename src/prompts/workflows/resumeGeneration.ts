/**
 * 简历生成状态工作流规则
 * 
 * 本规则仅负责前端状态控制、页面跳转和数据展示隔离
 * 不参与修改 Agent 原有业务逻辑
 * 
 * 触发关键词：「正在生成专属简历」
 */

export const RESUME_GENERATION_WORKFLOW = `

## 简历生成状态与页面跳转规则

当你的回复中需要触发生成简历时，必须包含以下关键词：

**「正在生成专属简历」**

包含此关键词后，前端将立即执行以下流程：

1. **立即触发前端生成动画页面跳转**，不等待你继续输出内容
2. **动画播放期间**，后台异步执行简历生成、数据处理及渲染准备流程
3. **聊天窗口禁止展示以下内容**：
   - JSON 数据
   - 简历完整内容
   - templateId、sections、personalInfo 等系统字段
   - 调试信息
   - 后台处理信息
4. **生成完成后**：
   - 自动进入简历展示页面
   - 使用后台生成的数据渲染对应简历模板
5. **异常处理**：
   - 若生成失败，不继续播放动画
   - 返回错误状态，引导用户重新生成或补充缺失信息

**执行要求**：
- 在确认信息足够、准备生成简历时，必须在回复中包含「正在生成专属简历」这句话
- 这句话之后，可以输出 [GENERATE_RESUME] 标记和 JSON 数据
- 前端会识别这句话并立即触发页面跳转，用户不会看到后续的 JSON 数据
`;

export default RESUME_GENERATION_WORKFLOW;
